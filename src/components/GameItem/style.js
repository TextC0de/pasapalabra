import styled from 'styled-components';

export const GameItem = styled.div`
    background: #fff;
    border-radius: 10px;
    box-shadow: 0 0 4px rgba(55, 55, 55, 0.32);
    margin: 0 0 1.5rem 0;
    overflow: hidden;
    text-align: center;
`;

export const Details = styled.div`
    text-align: left;
    padding: 1.875rem;
`;

export const Title = styled.h4`
    margin: 0;
`;

export const Description = styled.p`
    margin: 1rem 0;
`;
